import { Landing } from './Landing';

// New users land here. The Landing component redirects returning visitors
// straight to /world (see hasEnteredBefore).
export default function Page() {
  return <Landing />;
}
